# Create a project directory
mkdir adk-blog-agent
cd adk-blog-agent

# Create and activate a virtual environment
python -m venv.venv
source.venv/bin/activate


python blog_agent.py "The impact of generative AI on software development"

